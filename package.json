{
  "name": "dream-career-mvp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js",
    "crawl:roles": "node scripts/crawlBossRoleTree.mjs",
    "normalize:roles": "node scripts/normalizeBossRoleSystem.mjs",
    "build:role-config": "node scripts/buildRoleConfig.mjs",
    "update:role-system": "npm run crawl:roles && npm run normalize:roles && npm run build:role-config"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4",
    "react-router-dom": "^7.13.2"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "playwright": "^1.59.1",
    "vite": "^8.0.1"
  }
}
